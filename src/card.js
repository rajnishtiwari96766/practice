import React from 'react'
import details from './data'

const Card = () => {
  return (
    <div>
        {
        details.map((val,index)=>{
            
        })
        }
    </div>
  )
}

export default Card